output "master" {
  value = "${proxmox_vm_qemu.master[0].default_ipv4_address}"
}

{% for index in range(1, input_worker_count|int + 1) %}
output "worker{{ index }}" {
  value = "${proxmox_vm_qemu.worker{{ index }}[0].default_ipv4_address}"
}
{% endfor %}